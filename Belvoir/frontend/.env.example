# ===========================================
# CONFIGURAÇÕES DO BELVOIR E-COMMERCE
# ===========================================
# Copie este arquivo para .env.local e preencha com seus valores

# -------------------------------------------
# Google OAuth (Login com Google)
# -------------------------------------------
# Obter em: https://console.cloud.google.com/apis/credentials
# 1. Crie um projeto no Google Cloud Console
# 2. Ative a API "Google+ API" ou "Google Identity"
# 3. Crie credenciais OAuth 2.0 (Aplicativo Web)
# 4. Adicione as URLs autorizadas:
#    - http://localhost:5173 (desenvolvimento)
#    - https://seudominio.com (produção)

VITE_GOOGLE_CLIENT_ID=seu_client_id_aqui.apps.googleusercontent.com

# -------------------------------------------
# EmailJS (Sistema de Contato)
# -------------------------------------------
# Obter em: https://www.emailjs.com/
# 1. Crie uma conta gratuita (200 emails/mês grátis)
# 2. Adicione um serviço de email (Gmail, Outlook, etc)
# 3. Crie um template de email
# 4. Copie as credenciais abaixo

VITE_EMAILJS_SERVICE_ID=service_xxxxxx
VITE_EMAILJS_TEMPLATE_ID=template_xxxxxx
VITE_EMAILJS_PUBLIC_KEY=sua_public_key_aqui

# -------------------------------------------
# Shopify Storefront API
# -------------------------------------------
# IMPORTANTE: O site funciona em modo demo sem estas credenciais.
# Quando o cliente fornecer acesso ao Shopify, siga os passos abaixo.
#
# Como obter as credenciais:
# 1. Acesse o Shopify Admin da loja
# 2. Vá em Settings > Apps and sales channels
# 3. Clique em "Develop apps" (ou "Develop apps for your store")
# 4. Se necessário, ative o desenvolvimento de apps
# 5. Clique em "Create an app" e dê um nome (ex: "Belvoir Website")
# 6. Na aba "Configuration", clique em "Configure Storefront API scopes"
# 7. Selecione os seguintes escopos:
#    - unauthenticated_read_product_listings
#    - unauthenticated_read_product_inventory
#    - unauthenticated_read_product_pickup_locations
#    - unauthenticated_read_product_tags
#    - unauthenticated_read_selling_plans
#    - unauthenticated_read_customers (se usar login)
#    - unauthenticated_write_checkouts
#    - unauthenticated_read_checkouts
#    - unauthenticated_read_customer_tags
# 8. Salve as configurações
# 9. Na aba "API credentials", copie o "Storefront API access token"
#
# O domínio é o subdomínio do Shopify (ex: minha-loja.myshopify.com)

VITE_SHOPIFY_STORE_DOMAIN=
VITE_SHOPIFY_STOREFRONT_ACCESS_TOKEN=

# -------------------------------------------
# Modo de operação
# -------------------------------------------
# development = usa dados mock locais (padrão se Shopify não configurado)
# production = usa Shopify real
# VITE_MODE=development
